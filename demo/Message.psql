
namespace Models;

Message {

    user_id int references users(id);
    message text;

    static find_all_by_user_id($id) {
        SELECT * FROM messages WHERE user_id = $id
    }

}

say_hello_to($name) {
    SELECT 'hello' || $name
}
